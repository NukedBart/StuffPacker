<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Packer.Dialogs.AddNewItemDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Packer.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add An Item"
	PrimaryButtonText="➕ Add"
    CloseButtonText="↩ Cancel"
    DefaultButton="Primary">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,10"
                   Text="File: "/>
        <TextBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" 
                 Margin="10,0,0,10" x:Name="FilePathBox"/>
        <Button Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" 
                Margin="10,0,0,10" Content="🔍 Browse"
                Click="BrowseButton_Click"/>
        
        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,10"
                   Text="EmbedFS Path: "/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" VerticalAlignment="Center" 
                 Margin="10,0,0,10" x:Name="VirtualPathBox"/>
    </Grid>
</ContentDialog>
